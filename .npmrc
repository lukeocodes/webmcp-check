# Use a flat, hoisted node_modules layout. This is the most reliable layout for
# heavy binary packages like @sparticuz/chromium under Vercel's serverless file
# tracing (avoids symlink resolution issues at runtime).
node-linker=hoisted
